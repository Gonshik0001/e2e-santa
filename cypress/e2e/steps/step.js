import { Given, When, Then } from '@badeball/cypress-cucumber-preprocessor';

Given('the user is logged in', () => {
  // Ваша логика для авторизации пользователя
});

When('the user creates a new box', () => {
  // Ваша логика для создания новой коробки
});

Then('the box should be created successfully', () => {
  // Ваша логика для проверки успешного создания коробки
});
